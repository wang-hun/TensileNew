<UserControl x:Class="TensileNeW.RecipeNameDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="420"
             Background="{DynamicResource AppSurfaceBackgroundBrush}"
             FontFamily="Microsoft YaHei">
    <Border Padding="24"
            Background="{DynamicResource AppSurfaceBackgroundBrush}"
            BorderBrush="{DynamicResource AppLayoutBorderBrush}"
            BorderThickness="1"
            CornerRadius="4">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Text="请输入配方名称："
                       FontSize="17"
                       FontWeight="Bold"
                       Foreground="{DynamicResource AppMetricNameForegroundBrush}" />

            <TextBox Grid.Row="1"
                     x:Name="InputBox"
                     Height="38"
                     FontSize="16"
                     Margin="0,14,0,0"
                     Padding="8,0"
                     VerticalContentAlignment="Center"
                     KeyDown="InputBox_KeyDown" />

            <StackPanel Grid.Row="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,22,0,0">
                <Button Style="{StaticResource ButtonPrimary}"
                        Content="确定"
                        Width="88"
                        Height="34"
                        Margin="0,0,8,0"
                        Click="Ok_Click" />
                <Button Content="取消"
                        Width="88"
                        Height="34"
                        Click="Cancel_Click" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
