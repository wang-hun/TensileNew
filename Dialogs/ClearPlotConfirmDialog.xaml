<UserControl x:Class="TensileNeW.ClearPlotConfirmDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="440"
             Background="{DynamicResource AppSurfaceBackgroundBrush}"
             FontFamily="Microsoft YaHei">
    <Border Padding="24"
            Background="{DynamicResource AppSurfaceBackgroundBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Text="确认删除当前所有曲线吗？&#x0a;（试验中，正在绘制曲线会保留。）"
                       FontSize="17"
                       FontWeight="Bold"
                       TextWrapping="Wrap"
                       Foreground="{DynamicResource AppMetricNameForegroundBrush}" />

            <StackPanel Grid.Row="1"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,24,0,0">
                <Button Style="{StaticResource ButtonPrimary}"
                        Content="确定"
                        Width="88"
                        Height="34"
                        Margin="0,0,8,0"
                        Click="Ok_Click" />
                <Button Style="{StaticResource ButtonDanger}"
                        Content="取消"
                        Width="88"
                        Height="34"
                        Click="Cancel_Click" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
